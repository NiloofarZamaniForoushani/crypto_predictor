symbol: "BTC-USD"
start: "2018-01-01"
end: null   # null means "today" when you run it

horizon_days: 1       # predict t+1
target_type: "direction"  # direction | return

test_size: 0.20       # last 20% is test (holdout)
n_splits_cv: 5        # walk-forward splits on train set

optuna:
  n_trials: 50
  timeout_sec: null

artifacts_dir: "artifacts"
random_seed: 42
